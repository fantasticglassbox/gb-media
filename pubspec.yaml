name: gb_media
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Core dependencies
  get_it: ^7.2.0
  provider: ^6.0.5
  shared_preferences: ^2.2.0
  
  # Network
  http: ^1.1.0
  connectivity_plus: ^3.0.3
  
  # Media playback
#  chewie: ^1.8.5
#  video_player: ^2.8.0

  flutter_cache_manager: ^3.3.2
  path_provider: ^2.1.0
#  flutter_media3: ^1.0.0
  better_player: ^0.0.84

dev_dependencies:
  flutter_launcher_icons: ^0.13.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/translations/

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/gb-img.png"  # Path to your new app icon